// import { createSelector } from '@reduxjs/toolkit'

export const toggleMenu = state => state.global.menuMobile
export const showPopupSelector = state => state.global.ComingSoonPopup
export const detailPopupSelector = state => state.detailpopup
